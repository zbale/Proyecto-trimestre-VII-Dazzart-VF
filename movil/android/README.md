// Esta carpeta contiene configuraciones de Android que Expo/EAS Build utilizará

// El archivo network_security_config.xml permite tráfico HTTP a estos dominios:
// - 67.202.48.5 (backend)
// - localhost
// - 127.0.0.1

// El archivo AndroidManifest.xml declara usesCleartextTraffic para compatibilidad
